import React from "react";

const Users = (props) => {

    let users = ['user0', 'user1', 'user2', 'user3' , 'user4'];

    return (
        <div>
            Users:
            {users}
        </div>
    );
};

export default Users;